# Postgres (Railway)
DATABASE_URL=postgresql://user:pass@host:5432/db
# pune 1 dacă te conectezi la Postgres extern cu SSL (proxy-ul public Railway)
DATABASE_SSL=0

# Cheia endpoint-ului de cron
CRON_SECRET=schimba-ma

# Parola panoului de admin (/admin) — fără ea panoul e dezactivat
ADMIN_PASSWORD=schimba-ma-si-pe-mine

# Conectare pagini FB cu un click din panou (buton „Conectează cu Facebook"):
# din aplicația ta Meta (developers.facebook.com → App settings → Basic).
# În aplicație, la Facebook Login → Settings → Valid OAuth Redirect URIs,
# adaugă: https://<domeniul-tau>/admin/fb/callback
FB_APP_ID=
FB_APP_SECRET=

# OpenAI (fallback global; poți pune și cheie dedicată per site)
OPENAI_API_KEY=sk-...
BOTOSANEANUL_OPENAI_API_KEY=

# Facebook — page access token (NU user token)
BOTOSANEANUL_FB_PAGE_ID=
BOTOSANEANUL_FB_ACCESS_TOKEN=

# Opțional: suprascrie feed-ul default
BOTOSANEANUL_FEED_URL=https://www.botosaneanul.ro/rss

PORT=3000
